{
  "name": "wdr-project-get-all",
  "version": "1.0.0",
  "description": "Get all project with filter",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js --external:wdr-connect-db --external:wdr-models",
    "local": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.150"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.12",
    "esbuild": "^0.25.6",
    "typescript": "^5.8.3"
  }
}
